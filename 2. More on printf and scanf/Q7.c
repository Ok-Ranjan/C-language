//Input a ASCII code from the user and print its corresponding character.
#include<stdio.h>
#include<conio.h>
int main()
{
    int as;
    printf("Enter a ASCII code: ");
    scanf("%d",&as);
    printf("Its corresponding character is %c",as);
    getch();
    return 0;
}